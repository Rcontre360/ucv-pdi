{
  "name": "pdi-depth-workspace",
  "private": true,
  "workspaces": [
    "packages/app",
    "packages/core"
  ],
  "scripts": {
    "dev:app": "bun --filter app dev",
    "build:app": "bun --filter app build"
  },
  "dependencies": {
    "zod": "^3.23.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0"
  }
}
